services:
  discordreactbot:
    image: ${DOCKER_REGISTRY-}discordreactbot
    build:
      context: .
      dockerfile: DiscordReactBot/Dockerfile
    volumes:
      - ./config.json:/app/config.json
